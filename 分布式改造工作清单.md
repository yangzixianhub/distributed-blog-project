# 分布式博客改造 — 工作清单

本文档汇总三项改造方向的任务拆解，便于排期与验收。对应代码入口主要为：`bbs-rest`、`bbs-article-service`、`bbs-vue-ui`、`bbs-user-service`。

---

## 一、文章阅读：静态 HTML（Nginx）+ Redis

**目标**：正文高并发读尽量不经由 Java + Mongo 拼页；热点元数据可走 Redis，减轻下游压力。

### 设计与共识

- [ ] 明确访问形态：用户打开详情是「纯静态 URL」还是「SPA + 部分静态」，以及与现有路由 `/detail/:id` 的关系。
- [ ] 约定静态文件路径规则（例如 `/static/articles/{id}.html` 或按 slug），与 Nginx `root`/`alias` 一致。
- [ ] 梳理仅「已审核上线」文章生成静态页；草稿、待审核、下架的处理策略（不生成 / 删除文件 / 403）。

### 后端与生成链路

- [ ] 在审核通过、编辑上线、删除/下架等节点触发「生成 / 更新 / 删除」静态 HTML（可与现有 Mongo 中 `articleHtml` 对齐）。
- [ ] 处理配图与站内资源的引用路径，避免静态页资源 404。
- [ ] （可选）异步生成：消息队列或线程池，避免阻塞审核接口。

### Nginx 与部署

- [ ] 配置 Nginx 静态目录与缓存头（`Cache-Control` / `ETag` 等）。
- [ ] 多实例或多台 Nginx 时，静态文件同步策略（共享存储、`rsync`、部署流水线拷贝）。

### Redis（与本改造的配合）

- [ ] 明确 Redis 承载内容：例如文章元信息缓存、热点列表、或「id → 是否有静态页」等，键名与 TTL 规范。
- [ ] 缓存失效：与发文、改文、删文、改状态同一套事件驱动删除或更新。

### 统计与行为

- [ ] PV：静态化后若不再走原 `getById`+`isPv`，需单独设计上报（如异步接口、批量写、`img` 打点等）。
- [ ] 权限：匿名不可见审核中文章的校验，在静态场景下的等价实现。

### 验证

- [ ] 功能回归：详情内容与后台一致。
- [ ] 压测对比：改造前后详情链路延迟（P90/P95）、QPS、应用与 DB CPU（对照前文约定）。

---

## 二、点赞：Redis 缓冲 + 落库

**目标**：高峰写点赞时削峰；读点赞数/是否已赞优先走内存缓存，最终与 MySQL（`fs_like`）对齐。

### 方案选型（写清口径）

- [ ] 选择模式：**仅缓存读**（写仍同步 DB） vs **写先入 Redis 再异步/批量落库**（最终一致，工程量大）。
- [ ] 明确评论点赞（`LikeCommentService`）是否与文章点赞同一套策略。

### 数据与键设计

- [ ] 文章维度：总点赞数、（可选）用户是否已赞的缓存结构；评论维度同理（若改造）。
- [ ] 与现有「行存在 + `state` 取反」语义一致，避免重复点赞与并发乱序（建议 Lua 或单 key 串行思路）。

### 实现任务

- [ ] 在 `LikeServiceImpl` / `LikeCommentServiceImpl` 中接入 Redis，与现有 `LikePoMapper` 逻辑衔接或分阶段替换。
- [ ] 若异步落库：死信、重试、对账任务（定时比对 Redis 与 DB）。

### 风险与测试

- [ ] 多实例 `bbs-user-service` 下并发与幂等验证。
- [ ] Redis 故障降级：是否回源只读 DB 或短暂只读缓存策略。

---

## 三、搜索：Elasticsearch 改造

**目标**：替代 MySQL 标题 `LIKE '%…%'`；支持全文与更好的检索扩展性；与 MySQL/Mongo 主数据最终一致。

### 基础设施

- [ ] 部署 ES（课程环境可单节点；生产叙事可写分片与副本）。
- [ ] Spring 侧引入官方 Java API 或 Spring Data Elasticsearch，完成连接与基础健康检查。

### 索引与文档模型

- [ ] 定义索引 mapping：`articleId`、`title`、`正文摘要或全文`（从 Mongo 同步）、标签、`state`、时间、作者展示字段等。
- [ ] 中文分词插件（如 IK）选型与测试。

### 数据同步

- [ ] 增量：文章创建/更新/审核/删除时写入或删除 ES 文档（推荐异步 + 重试）。
- [ ] 全量：首次重建索引脚本或管理接口。
- [ ] （进阶）对账：DB 与 ES 定期抽样或全量校验。

### API 与前端

- [ ] 新增或改造搜索接口：ES 查询 → 返回 id 列表与排序 → 按需回源 MySQL/Mongo。
- [ ] 前端搜索框仍走 `/search` + `query` 参数时，改为调用新接口或网关聚合。
- [ ] 保留原列表排序等非搜索场景时，明确哪些仍走 `getList`。

### 验证

- [ ] 相关性、分页、空结果与降级（ES 不可用是否回退 MySQL LIKE）。
- [ ] 压测：搜索 QPS 与 P95，对比改造前。

---

## 四、跨项事项（建议三项都做时统一考虑）

- [ ] 配置与环境：`application.yml`、密钥、各环境 Redis / ES / Nginx 地址文档化。
- [ ] 观测：关键接口日志、慢查询、Redis/ES 监控（作业可简化）。
- [ ] 报告材料：架构简图、一致性说明（最终一致 vs 强一致）、压测前后表格。

---

## 文档修订说明

- 清单依据当前仓库典型链路整理：`ArticleServiceImpl.getByIds` / `getList`、`LikeServiceImpl`、`UserController` 点赞接口、标题搜索 `andTitleLike`。
- 实施时可按课程课时砍掉「异步落库」「CDC」等子项以控制工作量。
