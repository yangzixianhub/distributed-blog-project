# ES分布式改造

## 1. 改造目标

本轮 Elasticsearch 改造的核心目标是：

- 用 ES 替代 MySQL 标题 `LIKE '%关键字%'` 搜索
- 支持正文全文检索，而不只是标题模糊匹配
- 为后续的相关性优化、搜索扩展、分布式演进打基础
- 与 MySQL、Mongo 中的文章主数据保持最终一致

当前代码主要涉及：

- `bbs-rest`
- `bbs-article-service`
- `bbs-vue-ui`

---

## 2. 当前完成情况

### 2.1 基础设施

已完成：

- 已接入官方 `elasticsearch-java` 客户端
- 已完成 `ElasticsearchClient`、连接配置、基础索引初始化
- 已配置文章索引 `bbs_article`
- 已启用 IK 分词器

当前状态：

- ES 可访问
- 索引可创建
- 已支持重建索引

完成度评估：

- `85%`

### 2.2 索引与文档模型

已完成：

- 建立文章搜索索引 mapping
- 已覆盖以下字段：
  - `articleId`
  - `title`
  - `content`
  - `state`
  - `isDeleted`
  - `labelNames`
  - `createUser`
  - `createUserName`
  - `createTime`
- 正文内容从 Mongo 同步到 ES，而不是只写 MySQL 摘要

已支持的搜索字段权重：

- `title^4`
- `content`
- `labelNames^2`
- `createUserName^1.5`

完成度评估：

- `75%`

### 2.3 数据同步

已完成：

- 文章创建后触发 ES 增量写入
- 文章更新后触发 ES 增量写入
- 文章审核状态变更后触发 ES 增量同步
- 文章删除后触发 ES 文档删除
- 已提供全量重建接口 `POST /api/bbs/article/rebuildSearchIndex`

本轮提升：

- 已从“主流程直接写 ES”升级为“异步执行 + 简单重试”
- 已补充重建索引过程中的异常排查与兼容修复

当前仍未完成：

- 还不是 MQ 方案
- 没有死信队列
- 没有补偿任务
- 没有 DB/ES 定期对账

完成度评估：

- `70%`

### 2.4 API 改造

已完成：

- 新增独立搜索接口：`GET /api/bbs/article/search`
- 保留原列表接口：`GET /api/bbs/article/getList`
- 搜索接口已支持：
  - 关键字全文检索
  - 返回分页结果
  - 返回高亮标题
  - 返回高亮正文片段
- 新增搜索健康检查接口：`GET /api/bbs/article/searchHealth`

本轮修复过的问题：

- 登录态下公开搜索接口被权限拦截
- 控制器参数未显式命名导致 Spring MVC 参数绑定异常
- 旧索引与新查询字段不一致导致搜索回退
- 索引重建“表面成功但实际写入失败”问题

完成度评估：

- `85%`

### 2.5 前端接入

已完成：

- 前端 `/search?query=...` 已切到新的 ES 搜索接口
- 搜索页不再只依赖旧的 `getList`
- 搜索结果已可展示高亮内容

已验证：

- 搜索 `node` 可以命中正文中的文章
- 搜索接口返回结果正常

完成度评估：

- `80%`

### 2.6 搜索结果验证

当前已确认：

- ES 索引中已有文章文档
- 搜索接口已能返回命中结果
- 实际搜索 `node` 可命中 2 篇文章
- 返回数据中已包含：
  - `highlightTitle`
  - `highlightContent`

---

## 3. 当前完成度总结

按阶段评估如下：

- 基础设施：`85%`
- 索引与文档模型：`75%`
- 数据同步：`70%`
- API：`85%`
- 前端接入：`80%`
- 对账与运维化：`10%`

整体状态可以定义为：

> ES 搜索主链路已打通，已经具备“可搜索、可重建、可增量同步、前端可接入”的阶段性能力，但距离生产级最终一致和运维化仍有明显差距。

---

## 4. 本轮关键问题与处理记录

### 4.1 搜索接口无权访问

问题：

- 搜索接口虽然标了 `@NoNeedLogin`
- 但登录态请求仍会经过 `UrlAccessCheckInterceptor`
- 公开接口被误拦截

处理：

- 在 `UrlAccessCheckInterceptor` 中补充 `@NoNeedLogin` 判断逻辑

### 4.2 搜索接口服务器异常

问题：

- Spring MVC 无法绑定未显式命名的 `@RequestParam`
- 工程未开启 `-parameters`

处理：

- 为 `ArticleController` 中相关参数补全 `@RequestParam(value = "...")`

### 4.3 搜索为空

问题：

- 索引 mapping 是旧版
- 搜索代码已经按新版字段查询
- 查询失败后回退到 MySQL 标题 `LIKE`
- 搜索词在正文中存在、标题中不存在，所以返回为空

处理：

- 兼容旧索引查询
- 去掉导致旧索引失败的排序依赖
- 完成索引重建

### 4.4 重建成功但索引仍为空

问题：

- `rebuildSearchIndex` 接口返回的 `indexedCount` 只是“准备写入数量”
- 旧实现没有检查 `bulkResponse.errors()`
- 可能出现“ES 实际写入失败，但接口仍显示成功”

处理：

- 为 bulk 重建补充错误检查
- 若有单条写入失败，直接抛异常

### 4.5 createTime 写入兼容问题

问题：

- 索引文档加入 `createTime`
- 序列化与 ES 日期字段存在兼容风险

处理：

- 将 `createTime` 写入格式调整为更稳定的时间字符串格式

---

## 5. 还未完成的部分

以下内容仍未完成，或者只完成了雏形：

- 异步同步仍是应用内线程池方案，不是 MQ 方案
- 无死信、无补偿、无消费失败持久化
- 无 DB 与 ES 对账任务
- 无抽样校验、无全量校验
- 无索引别名切换方案
- 无零停机重建方案
- 搜索排序仍较基础，未引入时间衰减与更复杂相关性策略
- 无联想搜索、拼写纠错、热门查询推荐
- 健康检查尚未真正接入统一监控

---

## 6. 当前风险

当前主要风险如下：

- 应用内异步同步在服务重启时仍可能丢失少量增量事件
- 若增量同步失败，目前缺少自动补偿闭环
- 重建索引虽可用，但还没有生产级切换策略
- ES 与主库的一致性仍属于“最终一致雏形”，未达到严格工程化标准

---

## 7. 下一步计划

建议按以下顺序推进：

### 第一优先级

- 增加 DB 与 ES 对账任务
- 增加重建后的数量校验与抽样校验
- 增加同步失败的可观测日志与告警

### 第二优先级

- 将增量同步升级为 MQ 驱动
- 引入失败重试、死信、补偿机制

### 第三优先级

- 为索引重建加入 alias 切换
- 支持无损重建与平滑切流

### 第四优先级

- 优化搜索相关性排序
- 增加更好的高亮策略
- 支持更多搜索扩展能力

---

## 8. 阶段性结论

截至当前，ES 改造已经完成了“第一阶段主链路打通”：

- 后端已可用
- 前端已接入
- 索引可重建
- 实际搜索已命中结果

因此，这一阶段可以认为：

> Elasticsearch 改造已从“技术预研/原型”进入“可交付的阶段性版本”，但还未进入“生产级最终一致与运维化完成状态”。

