<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sang.user.mapper.UserMapper">
    <select id="loadUserByUsername" resultMap="BaseResultMap">
        SELECT u.*, r.`id` AS rid, r.`name` AS rname
        FROM user u
        LEFT JOIN roles_user ru ON u.`id` = ru.`uid`
        LEFT JOIN roles r ON r.`id` = ru.`rid`
        WHERE u.username = #{username}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, nickname, email, enabled, regTime)
        VALUES(#{username}, #{password}, #{nickname}, #{email}, #{enabled}, NOW())
    </insert>

    <update id="updateUserEmail">
        UPDATE user SET email = #{email} WHERE id = #{id}
    </update>

    <select id="getUserByNickname" resultMap="BaseResultMap">
        SELECT u.*, r.`id` AS rid, r.`name` AS rname
        FROM user u
        LEFT JOIN roles_user ru ON u.`id` = ru.`uid`
        LEFT JOIN roles r ON r.`id` = ru.`rid`
        <where>
            <if test="nickname != null and nickname != ''">
                u.nickname LIKE concat('%', #{nickname}, '%')
            </if>
        </where>
        ORDER BY u.`id`
        <if test="nickname == null or nickname == ''">
            LIMIT 20
        </if>
    </select>

    <select id="getUserById" resultMap="BaseResultMap">
        SELECT u.*, r.`id` AS rid, r.`name` AS rname
        FROM user u
        LEFT JOIN roles_user ru ON u.`id` = ru.`uid`
        LEFT JOIN roles r ON r.`id` = ru.`rid`
        WHERE u.`id` = #{id}
    </select>

    <resultMap id="BaseResultMap" type="org.sang.user.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="nickname" property="nickname"/>
        <result column="password" property="password"/>
        <result column="enabled" property="enabled"/>
        <result column="email" property="email"/>
        <result column="userface" property="userface"/>
        <result column="regTime" property="regTime"/>
        <collection property="roles" ofType="org.sang.user.entity.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
        </collection>
    </resultMap>

    <update id="updateUserEnabled">
        UPDATE user SET enabled = #{enabled} WHERE id = #{uid}
    </update>

    <delete id="deleteUserById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <delete id="deleteUserRolesByUid">
        DELETE FROM roles_user WHERE uid = #{id}
    </delete>

    <insert id="setUserRoles">
        INSERT INTO roles_user(rid, uid) VALUES
        <foreach collection="rids" separator="," item="rid">
            (#{rid}, #{id})
        </foreach>
    </insert>
</mapper>
